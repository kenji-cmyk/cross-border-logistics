import type { InputHTMLAttributes, ReactNode, TextareaHTMLAttributes } from "react";
import { cn } from "../../lib/cn";
export function Field({ label, id, error, hint, suffix, children, ...props }: InputHTMLAttributes<HTMLInputElement> & { label: string; id: string; error?: string; hint?: string; suffix?: string; children?: ReactNode }) {
  return <div><label htmlFor={id} className="mb-2 block text-sm font-semibold text-ink">{label}{props.required && <span className="ml-1 text-danger" aria-hidden="true">*</span>}</label><div className="relative"><input id={id} aria-invalid={Boolean(error)} aria-describedby={error ? `${id}-error` : hint ? `${id}-hint` : undefined} className={cn("min-h-12 w-full rounded-2xl border bg-white px-4 py-3 text-sm text-ink outline-none transition placeholder:text-subtle focus:border-brand focus:ring-4 focus:ring-brand/10", error ? "border-danger" : "border-black/10", suffix && "pr-12")} {...props} />{suffix && <span className="pointer-events-none absolute inset-y-0 right-4 flex items-center text-sm font-medium text-muted">{suffix}</span>}{children}</div>{hint && !error && <p id={`${id}-hint`} className="mt-2 text-xs leading-5 text-muted">{hint}</p>}{error && <p id={`${id}-error`} role="alert" className="mt-2 text-xs font-medium text-danger-dark">{error}</p>}</div>;
}
export function TextAreaField({ label, id, error, hint, ...props }: TextareaHTMLAttributes<HTMLTextAreaElement> & { label: string; id: string; error?: string; hint?: string }) { return <div><label htmlFor={id} className="mb-2 block text-sm font-semibold">{label}{props.required && <span className="ml-1 text-danger">*</span>}</label><textarea id={id} aria-invalid={Boolean(error)} className={cn("min-h-32 w-full resize-y rounded-2xl border bg-white px-4 py-3 text-sm outline-none focus:border-brand focus:ring-4 focus:ring-brand/10", error ? "border-danger" : "border-black/10")} {...props} />{hint && !error && <p className="mt-2 text-xs text-muted">{hint}</p>}{error && <p role="alert" className="mt-2 text-xs font-medium text-danger-dark">{error}</p>}</div>; }
