import { Menu, X } from "lucide-react";
import { useState } from "react";
import { Link } from "react-router-dom";
const links = [["Home", "#home"], ["How It Works", "#how-it-works"], ["Tracking", "#tracking"], ["Rates", "/rates"], ["About", "#about"]];
export function Navbar() { const [open, setOpen] = useState(false); return <header className="relative z-20 mx-auto max-w-7xl px-5 py-5 lg:px-8"><nav aria-label="Primary navigation" className="relative flex items-center justify-between rounded-[2rem] border border-black/10 bg-white/65 px-5 py-3 shadow-soft backdrop-blur-xl"><a href="#home" className="flex items-center gap-3 rounded-md focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand"><span className="font-display text-2xl tracking-tight text-ink sm:text-3xl">CrossBorder<sup className="ml-0.5 align-super font-sans text-[.35em]">®</sup></span><span className="hidden border-l border-black/10 pl-3 font-sans text-[9px] font-medium uppercase leading-tight tracking-[.2em] text-muted sm:block">Shopping<br />&amp; Logistics</span></a><div className="hidden items-center gap-7 lg:flex">{links.map(([label, href]) => href.startsWith("/") ? <Link key={label} to={href} className="text-sm font-medium text-muted transition-colors hover:text-ink">{label}</Link> : <a key={label} href={href} className="text-sm font-medium text-muted transition-colors hover:text-ink">{label}</a>)}</div><div className="flex items-center gap-2"><Link to="/quote" className="hidden rounded-full bg-ink px-6 py-2.5 text-sm font-medium text-white transition-transform duration-200 hover:scale-[1.03] active:scale-[.98] focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-brand focus-visible:ring-offset-2 sm:block">Get a Quote</Link><button type="button" aria-expanded={open} aria-controls="mobile-menu" aria-label="Toggle navigation menu" onClick={() => setOpen(!open)} className="grid h-11 w-11 place-items-center rounded-full border border-black/10 bg-white/70 lg:hidden">{open ? <X className="h-5 w-5" /> : <Menu className="h-5 w-5" />}</button></div>{open && <div id="mobile-menu" className="absolute left-3 right-3 top-[calc(100%+.6rem)] grid gap-1 rounded-2xl border border-black/10 bg-white/95 p-3 shadow-xl backdrop-blur-xl lg:hidden">{links.map(([label, href]) => href.startsWith("/") ? <Link key={label} to={href} onClick={() => setOpen(false)} className="rounded-xl px-4 py-3 text-sm font-medium text-muted hover:bg-brand-soft hover:text-ink">{label}</Link> : <a key={label} href={href} onClick={() => setOpen(false)} className="rounded-xl px-4 py-3 text-sm font-medium text-muted hover:bg-brand-soft hover:text-ink">{label}</a>)}<Link to="/quote" className="rounded-xl bg-ink px-4 py-3 text-center text-sm font-medium text-white sm:hidden">Get a Quote</Link></div>}</nav></header>; }
