import { CloudOff, Compass } from "lucide-react";
import { Link } from "react-router-dom";
import { AppShell, PageContainer } from "../app/layouts/AppShell";
function SystemPage({ unavailable = false }: { unavailable?: boolean }) { return <AppShell><PageContainer narrow><section className="rounded-[2rem] bg-white p-8 text-center shadow-card sm:p-12"><span className="mx-auto grid h-14 w-14 place-items-center rounded-2xl bg-brand-soft text-brand">{unavailable ? <CloudOff /> : <Compass />}</span><p className="mt-6 text-xs font-semibold uppercase tracking-[.18em] text-brand">{unavailable ? "System unavailable" : "404 · Page not found"}</p><h1 tabIndex={-1} className="mt-3 font-display text-5xl tracking-tight">{unavailable ? "We cannot connect right now." : "This route went off course."}</h1><p className="mx-auto mt-5 max-w-xl text-sm leading-6 text-muted">{unavailable ? "The gateway or one of its connected services is temporarily unavailable. Your saved identifiers remain in the browser." : "The page may have moved, or the link may be incomplete. Return home or start a new quotation."}</p><div className="mt-8 flex flex-col justify-center gap-3 sm:flex-row"><Link to="/" className="inline-flex min-h-12 items-center justify-center rounded-2xl bg-ink px-6 text-sm font-semibold text-white">Return home</Link><Link to="/quote" className="inline-flex min-h-12 items-center justify-center rounded-2xl border border-black/10 bg-white px-6 text-sm font-semibold">Start a quotation</Link></div></section></PageContainer></AppShell>; }
export function NotFoundPage() { return <SystemPage />; }
export function SystemUnavailablePage() { return <SystemPage unavailable />; }
